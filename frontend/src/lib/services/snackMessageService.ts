
export const showFileUploadedMessage = async () => {
  // snackSliceの呼び出し
};

export const showFileUploadFailedMessage = async () => {
  // snackSliceの呼び出し
};

export const showLoginMessage = async () => {
  // snackSliceの呼び出し
};

export const showLogoutMessage = async () => {
  // snackSliceの呼び出し
};

export const showAuthFailedMessage = async () => {
  // snackSliceの呼び出し
};
